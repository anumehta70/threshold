# Leave unset to run Threshold in local demo-ledger mode (default — fully
# clickable without a deployed contract). Set this after you deploy to
# Preprod to switch the app to network mode.
VITE_CONTRACT_ADDRESS=
